{
  "name": "blindbook",
  "version": "0.1.0",
  "description": "BlindBook — Encrypted On-Chain Order Book using FHE on Fhenix",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network eth-sepolia"
  },
  "keywords": [
    "fhe",
    "fhenix",
    "orderbook",
    "defi",
    "privacy"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.28.6"
  },
  "dependencies": {
    "@cofhe/hardhat-plugin": "^0.4.0",
    "@cofhe/mock-contracts": "^0.4.0",
    "@cofhe/sdk": "^0.4.0",
    "@fhenixprotocol/cofhe-contracts": "^0.1.3",
    "dotenv": "^17.3.1"
  }
}
